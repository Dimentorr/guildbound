## Актуальная сборка: 0.3.6.3-dev

[Размер чисел и положение уровня HUD](RELEASE_0.3.6.3.md).

## Предыдущая сборка: 0.3.6.2-dev

[Высота полос HP/MP в HUD](RELEASE_0.3.6.2.md).

## Предыдущая сборка: 0.3.6.1-dev

[Исправления шрифтов и полной заливки](RELEASE_0.3.6.1.md). Сборка и 111 тестов прошли; визуальная проверка клиентом не выполнена.

## Предыдущая сборка: 0.3.6-dev

[Изменения и проверки](RELEASE_0.3.6.md). 111 тестов и четыре серверных сценария с Terralith прошли. Внешний вид в клиенте требует проверки.

# Статус первого технического прототипа

## 0.3.0-dev — расширение подклассов

Актуальное описание, баланс, ограничения и ручной сценарий: [RELEASE_0.3.0.md](RELEASE_0.3.0.md). 17 подклассов, 30 активных умений, фокусы магии, призывы, классовая экипировка и пять ежедневных контрактов. Протокол 9. Серверные сценарии с Terralith прошли, включая применение всех умений и отклонение повторов. Визуальная проверка клиентом предстоит.

## Версия 0.2.1-dev: опыт, дерево от центра и узкий гобелен

- Цена 1→2 теперь 100 XP; остальные цены прежние, до уровня 5 всего 1188 XP. Пороговые unit-тесты и ожидаемый остаток серверного сценария обновлены.
- Начальный масштаб дерева до 2×, с адаптацией под размеры видимой области: корень и первые узлы помещаются при маленьком окне. Корень по центру, общие ветви растут влево/вправо, дочерние узлы дальше наружу. Масштабирование относительно курсора, перетаскивание и возврат в центр сохранены. Круговое дерево с переходами между ветвями записано как будущая задача без добавления новых зависимостей сейчас.
- Гобелен сужен с 80 до 40 единиц в HUD и меню. Вместо текста — рисованная кодом эмблема щита/звезды, цвет и украшения ранга. Подпись показывает текущий totalLevel, а не уровень открытия ранга. В стойке появилась кнопка «Ранг гильдии»: название, текущий уровень, следующий порог, статус привилегий. Привилегии ещё не реализованы и прямо отмечены как отсутствующие.
- Сборка успешна; 88 тестов без ошибок, включая попадание первых узлов в начальный вид на разных размерах окна. 180 ключей RU/EN, подстановки совпадают. compileSmokeJava прошёл; выделенный сервер повторно не запускался, сетевой протокол остался 8. Визуальный клиентский тест не выполнен.
- JAR: build/libs/guildbound-0.2.1-dev.jar, 190462 байта; SHA-256 5170AE08C9E766AE0F7A8C0D64B8D3DC2E39DF4A1D24C391A2E64B3735F2DF08.

## Версия 0.2.0-dev: классы, деревья и ранги, 21 сентября 2026

- Регистрация выбирает только базовый класс; прежние активные способности и бонусы уровня 3 сохранены. С уровня класса 5 у стойки доступен подкласс: Охотник / Дуэлянт / Защитник / Чародей. Пока по одному реализованному подклассу на класс, дополнительные варианты из ТЗ остаются будущим контентом.
- Два отдельных дерева навыков вместо списка. Узлы соединены по зависимостям, колесо масштабирует вокруг курсора, перетаскивание пустого места или ПКМ двигает дерево, есть сброс камеры и панель требований с отдельной покупкой.
- Начальные 5 очков доступны для общего дерева на уровне 1. Далее +1 на 2–5, +2 на 6–10, +3 на 11–20, +5 на 21–45, +10 на 46–74, +20 на 75–89, +25 на 90–99, +50 на 100. Итоговый бюджет 1064, зависит от текущего уровня, не дублируется входом/сменой пути. Текущие узлы имеют два ранга стоимостью 1/2 очка. Сохранённые покупки пересчитаны без удаления.
- Общее дерево: Живучесть → Стойкость (−2% боевого урона/ранг), Второе дыхание → Медитация (+2 маны/с/ранг). В классовых деревьях сохранены прежние узлы и добавлена соответствующая ветвь подкласса уровней 5/7: ещё +5% урона лука, +5% скорости кинжалов, −3% боевого урона или +0,5 сердца исцеления за ранг. Дочерним узлам нужен первый ранг родителя.
- Максимум RPG-уровней повышен до 100. Цены достижения уровней 2–5: 113/225/338/525 XP, всего 1201; далее 21 × L². Награды мобов/квестов прежние. Старый опыт переносится через новые пороги при входе. Выбор мультикласса пока закрыт.
- Все девять рангов по уровням пользователя. Гобелен появляется с пятого справа за пределами окна персонажа и компактно справа от HUD. После специализации знак класса меняется. Ранг пересчитывается и вниз при смене пути, бонусов характеристик не даёт.
- Формат прогресса v2 читает v1; старый класс сохраняется, подкласс ниже 5 снимается, с 5 сохраняется. Серверная проверка выбора использует токен стойки, срок, измерение, дистанцию и видимость; повторы запрещены. Протокол 8 требует обновить обе стороны.
- Итоговая сборка успешна: 87 JUnit-тестов, 0 ошибок; 174 ключа локализации RU/EN, подстановки совпадают. Проверены все пороги XP/рангов/очков, первые 5 очков, стоимость и зависимости, миграция, сохранения и масштабирование.
- Выделенный сервер NeoForge 21.1.250 + Terralith 2.5.8 прошёл прежний боевой сценарий и новый сценарий специализации: запрет на уровне 4, переход на 5, 9 очков, неверный токен/класс/дистанция, успешный выбор, отказ повтору, сохранение/загрузка. Консольные маркеры: `GUILDBOUND_PROGRESSION_V2_PASS` и `GUILDBOUND_COMBAT_SMOKE_PASS`. Сервер сохранён и штатно остановлен; лог `build/reports/verification/server-0.2.0-with-terralith.log`. После этого добавлено только компактное отображение гобелена в HUD; итоговый build повторно прошёл.
- JAR: `build/libs/guildbound-0.2.0-dev.jar`, 187 096 байт. SHA-256: `73DA974929F11F5B9F31F4F8E3BF53798177B23DAD243C9F87BEA31D36FF0A7D`.
- Реальная визуальная проверка клиента не выполнялась. Список ручной проверки — в начале TESTING.md. Деревья содержат начальные ветви, а не полный набор способностей до уровня 100; свободные очки можно сохранять на будущие узлы.

## Версия 0.1.9-dev: общий откат и лечение игроков, 21 сентября 2026

- Пользователь уточнил: заговоры должны иметь общий откат. Прежде переключение отображало готовность другого независимого таймера; теперь максимум старых сохранённых таймеров блокирует оба каста. После искры лечение ждёт её оставшееся время, после лечения оба ждут 5 секунд. Переключение не сбрасывает/не перезапускает откат. HUD и обе карточки круга показывают общий таймер, сервер отклоняет попытки обхода.
- Добавлено лечение игрока в прицеле до 12 блоков: сервер сам выбирает цель, препятствия и ближайшие существа перекрывают луч. Без подходящей цели — самолечение, приседание принудительно выбирает себя. HUD показывает имя/HP получателя. Полностью здоровая цель ничего не тратит; цена и сила лечения, включая талант, прежние. Пока разрешён любой игрок, групповые ограничения/опыт поддержки ещё не реализованы.
- Протокол 7 требует обновления клиента и сервера. Сохранения ресурсов/классов/журнала прежние; общий откат использует уже существующие таймеры без миграции.
- Сборка успешна, 77 тестов без ошибок/пропусков, совпадают 139 ключей RU/EN и подстановки. Новые unit-тесты проверяют общий запрет от обоих таймеров и истечение максимального сохранённого остатка.
- Сервер с Terralith прошёл прежнюю регрессию и новые FakePlayer-сценарии: искра блокирует лечение, лечение блокирует искру, повторный каст отклонён, целевой игрок получает 6 HP с талантом, маг не лечится за него, списывается ровно 25 маны, приседание лечит себя, полная цель не расходует ресурсы, стена и дальность 20 блоков исключают целевое лечение. Маркер: `GUILDBOUND_COMBAT_SMOKE_PASS: prior regression, shared cantrip lock in both directions, ally healing, self override, mana, wall and range`.
- Сервер сохранён и штатно остановлен. Лог: `build/reports/verification/server-0.1.9-with-terralith.log`; консольный маркер System.out зафиксирован здесь отдельно.
- JAR: `build/libs/guildbound-0.1.9-dev.jar`, 165 746 байт. SHA-256: `D2E1CED9BD427532382F5A6FD757D8FFF0580092C67410E47FDE2AB027A98B2D`.
- Пользователь подтвердил управление 0.1.8. Новый HUD получателя и общий таймер в круге требуют ручной клиентской проверки, как и целевое лечение при задержке сети.

## Версия 0.1.8-dev: единая клавиша магии, 21 сентября 2026

- Отдельные клавиши лечения/искры заменены переназначаемой «Магией» (по умолчанию V). Короткое нажатие кастует выбранный заговор на отпускании; удержание 250 мс открывает круг. Выбор мышью, отпускание сохраняет без каста; центр сохраняет прежнее, Esc отменяет. Потеря фокуса/переход в другой экран отменяет ожидающий каст, отпускание после долгого удержания не превращается в атаку при задержках клиентских тиков.
- Два заговора расположены справа/слева. RadialLayout рассчитывает равномерное размещение и выбор направления для произвольного числа элементов; тесты покрывают 2–12. MagicSpell содержит circle=0 для заговоров; круги обозначены как будущий раздел, сами заклинания кругов и страницы большого гримуара ещё не реализованы.
- HUD показывает рывок и один выбранный заговор. Выбор хранится в клиентской конфигурации; переключение не меняет серверные ресурсы, урон или откаты. Общая клавиша поддерживает переназначение; её фактическая подпись используется в HUD, круге и обучении. Старые раздельные назначения G/V больше не зарегистрированы.
- При выборе мага/первом показе существующему магу открывается визуальная инструкция: два значка, схема мыши, анимированная подсветка направления и объяснение нажатия/удержания. Повторный вызов — K → Способности → «Как выбирать магию?». Мир не ставится на паузу.
- Сборка успешна, 75 тестов без ошибок/пропусков. Добавлены тесты короткого нажатия, удержания, задержанного отпускания, отмены фокусом и геометрии. Совпадают 135 ключей RU/EN и подстановки. Сервер повторно не запускался: сетевые действия и протокол 6 прежние; это клиентская переработка управления. Реальные ввод/анимация/раскладки/масштабы ещё требуют ручной проверки.
- JAR: `build/libs/guildbound-0.1.8-dev.jar`, 161 324 байта. SHA-256: `7EE60F0F3691C0D8B2C29D0DBAE8FEFA80B69280416CA61CAC93F30FE6F978F4`.

## Исправление журнала: 0.1.7.4-dev, 21 сентября 2026

- Через клавишу меню показываются только принятые, ещё не сданные контракты. Выполненная цель остаётся в журнале с печатью до получения награды; кнопок принятия/сдачи вне стойки нет.
- У стойки видны доступный для принятия контракт или текущее принятое задание для проверки/сдачи. Будущие и сданные задания скрыты. В существующей последовательной цепочке доступен один контракт за раз; после сдачи автоматически выбирается следующий.
- Пустые состояния журнала и стойки имеют отдельные пояснения. Скрытые карточки отключены, видимая располагается первой; устаревшее выделение не оставляет сданное задание на экране.
- Сборка успешна, 70 существующих тестов без ошибок/пропусков, 123 ключа RU/EN совпадают. Клиентские переходы/фокус требуют ручной проверки. Сервер повторно не запускался; изменения клиентские, протокол 6 и сохранения прежние.
- JAR: `build/libs/guildbound-0.1.7.4-dev.jar`, 147 429 байт. SHA-256: `AC939D060CCADAD8ED5C4FB89D08D9AB1F4086AFA33403272D9FC11CF54F54A6`.

## Исправление интерфейса: 0.1.7.3-dev, 21 сентября 2026

- Таймер способностей рисуется на слое +200 с завершением предыдущей отрисовки, поверх иконки предмета (ванильный `renderItem` использует +150). Значок больше не должен закрывать цифры.
- Названия контрактов рисуются по центру без чёрной тени, вместо ванильного `renderScrollingString`; длинная надпись вписывается по ширине. Это устраняет наложение чёрной тени на чёрные буквы, показанное на скриншоте пользователя.
- Добавлена красная печать «Выполнено» на карточках и листе выбранного задания. Она появляется при выполненной цели и сохраняется после сдачи. Отдельный статус по-прежнему различает возвращение за наградой и уже полученную награду.
- Сборка успешна, 70 существующих тестов без ошибок/пропусков, совпадают 120 ключей RU/EN. Сервер повторно не запускался: изменения только клиентской отрисовки и локализации. Протокол 6 и сохранения прежние. Визуальный результат ещё требует проверки настоящим клиентом.
- JAR: `build/libs/guildbound-0.1.7.3-dev.jar`, 147 003 байта. SHA-256: `406789F952DE60C9E7E626AA65762A0935F0D3DA16F58D51A900FEDC4F9728B0`.

## Завершённая версия: 0.1.7.2-dev, 21 сентября 2026

- Искра: 5 базового урона, 2,5 с отката, без маны. С уровня мага 5 автоматически +1,5 урона независимо от талантов. Отдельный EMBER на уровнях 3/5 за одно очко за ранг: +1 урона и −0,5 с за каждый из двух рангов. Максимум в прототипе: 8,5 урона / 1,5 с. У мага четыре строки талантов; описание использует серверную формулу, HUD показывает десятые доли секунды.
- По подтверждению пользователя первый уровень остаётся бесплатным при регистрации, переходы к 2/3/4/5 стоят 150/300/450/700 XP. До пятого — 1600 XP. Уже достигнутые уровни и накопленный опыт не пересчитываются; награды мобов/контрактов прежние. Кривая после пятого не вводилась.
- Новый талант сохраняется прежним именованным codec; диапазон сохранённого отката расширен до 50 тиков и читает старые значения. Протокол 6 требует обновить обе стороны. Сброс талантов снимает EMBER, но не сбрасывает уже идущий откат.
- `build` успешен, 70 JUnit-тестов без ошибок/пропусков, JSON и 119 ключей RU/EN проверены. Новые тесты проверяют точные пороги/1600 XP, сохранённый прогресс, урон каждого ранга, независимый бонус пятого уровня, класс/уровень/цену таланта.
- Сервер с Terralith прошёл прежнюю регрессию и новые проверки: 5 урона базовым снарядом, откат 50 тиков с сохранением, 8,5 урона и 30 тиков с EMBER 2 на пятом уровне, сохранение нового таланта, пересчитанная награда опыта в цепочке контрактов. Консольный маркер `GUILDBOUND_COMBAT_SMOKE_PASS` получен. Сервер сохранён и штатно остановлен; лог `build/reports/verification/server-0.1.7.2-with-terralith.log`.
- JAR: `build/libs/guildbound-0.1.7.2-dev.jar`, 146 121 байт. SHA-256: `3B378F6FEFB049FFFFF2250C2FB252C94F45D18D10421CC9AEB208452C33C556`.
- Четыре строки талантов и восприятие нового темпа прокачки ещё требуют ручной проверки.

## Завершённая версия: 0.1.7.1-dev, 21 сентября 2026

- Контракты выбираются кликом по трём карточкам, выбранная подчёркнута; отдельный лист показывает описание, статус и награду чёрным текстом на процедурном пергаменте. Непринятое задание явно предлагает принятие; действие применяется только к выбранному доступному контракту. Будущие и сданные контракты можно читать.
- Одиночный кинжал во второй руке работает по ЛКМ с пустой основной рукой или другим предметом в ней: собственные характеристики, прочность, полный откат и расход стамины; без бонуса двух кинжалов. У живой цели кинжал получает приоритет над предметом основной руки, добыча блоков остаётся ванильной.
- Маг: «Огненная искра» на V, переназначение, иконка и откат в HUD, описание способностей. Без маны, 1 с между выстрелами, 6 урона / 7 с уровня мага 3. Отдельный серверный снаряд с визуальным огнём, без поджога, взрыва и разрушения блоков. Баланс относительно следопыта предварительный.
- Протокол 5 требует обновить клиент и сервер. Откат заговора сохраняется отдельным attachment, смена пути его не сбрасывает. Классы, таланты и журнал сохраняют прежние форматы.
- Сборка успешна: 67 JUnit-тестов без ошибок/пропусков, 24 JSON-файла и 117 ключей RU/EN с одинаковыми подстановками. Smoke-код исключён из JAR.
- Сервер с Terralith прошёл прежнюю регрессию и новые сценарии: урон/износ одиночного кинжала второй руки, отказ спаму, реальный полёт и попадание искры (6 HP), отсутствие горения цели, отсутствие расхода маны, сохранение отката, отказ немагу, столкновение с шерстью без её разрушения/поджога.
- Консольный маркер: `GUILDBOUND_COMBAT_SMOKE_PASS: prior regression, offhand-only dagger, cantrip hit, cooldown, persistence, class gate, no ignition or terrain damage`. Сервер сохранён и штатно остановлен. Лог: `build/reports/verification/server-0.1.7.1-with-terralith.log`; маркер System.out зафиксирован здесь отдельно.
- JAR: `build/libs/guildbound-0.1.7.1-dev.jar`, 144 691 байт. SHA-256: `DEFD3FAE69CD9881FF148B1ED060A56579E90588A0C9EAA43CB527EFF3312086`.
- Пользователь подтвердил начальные квесты и награды 0.1.7. Пергамент, клиентский рендер снаряда, управление и сравнительный баланс 0.1.7.1 требуют ручной проверки; серверный тест их не заменяет.

## Завершённая версия: 0.1.7-dev, 21 сентября 2026

- По пожеланию пользователя релиз охватывает связанный игровой цикл контрактов, а не отдельную небольшую механику. В UI «Пересборка» заменена на «Смена пути», штраф вынесен в красный текст, нейтральные сведения отделены.
- Три последовательных личных контракта: «Ночной дозор» (5 враждебных мобов, 100 XP / 2 изумруда), «Неупокоенные» (6 обычных зомби, 150 XP / 3 изумруда), «Костяная угроза» (4 обычных скелета, 200 XP / 4 изумруда). Получение и сдача у стойки, просмотр через K, компактный отключаемый счётчик под HUD.
- Журнал сохраняется при выходе, смерти и смене пути. Сервер проверяет сессию/дистанцию/видимость стойки, авторство убийства, цель и состояние контракта; повторная сдача не выдаёт награду. Лимит уровня 5 прежний. Задания не повторяются, группового зачёта и ассистов пока нет. Creative не продвигает цели.
- Протокол 4: обновить клиент и сервер. Старые персонажи получают пустой журнал без сброса класса, талантов или ресурсов.
- `build` успешен: 65 JUnit-тестов, 0 ошибок, 0 пропусков. Проверены 24 JSON-файла, совпадение 113 ключей RU/EN и подстановок. Smoke-классы отсутствуют в JAR.
- Сервер NeoForge с Terralith 2.5.8 загрузился и прошёл FakePlayer-сценарии боя, талантов, сохранений, смены пути и всей цепочки контрактов: отказ ранней сдаче, отказ вдали от стойки, повтор принятия, зачёт убийств через обработчик смерти, загрузка готового журнала, точные XP/изумруды, отказ повторной награде, фильтрация целей и завершение всех трёх контрактов. Смена пути сохраняет активный журнал.
- Консольный маркер: `GUILDBOUND_COMBAT_SMOKE_PASS: combat, talents, persistence, path change, quest acceptance, objectives, rewards, replay and full chain`. Сервер сохранён и штатно остановлен; лог загрузки/остановки: `build/reports/verification/server-0.1.7-with-terralith.log` (System.out-маркер хранится в этом отчёте, не в логе).
- JAR: `build/libs/guildbound-0.1.7-dev.jar`, 136 479 байт. SHA-256: `68CE6D1EFFC39F0248C81872F10BD8913D4FE3532364D1C1C67991F9B12B20E5`.
- Визуальные проверки, переполненный инвентарь, смерть настоящего игрока и совместная игра требуют ручного маршрута из TESTING.md. Серверный тест не подменяет их.
- Пользователь подтвердил работу 0.1.6; новые механики 0.1.7 ещё не проверены вручную.

## Завершённая версия: 0.1.6-dev, 21 сентября 2026

- Пересборка у стойки гильдии: выбор прежнего или другого класса, экран последствий и отдельное подтверждение. Цена прототипа — один RPG-уровень, минимум 1; опыт текущего уровня и таланты сбрасываются. Очки вычисляются по оставшемуся уровню. Новая экономика из ТЗ пока отложена.
- Старые бонусы снимаются сразу, здоровье ограничивается новым максимумом без лечения. Инвентарь, прочность, ресурсы и перезарядки сохраняются.
- Сервер проверяет стойку, дистанцию, видимость, измерение, состояние персонажа и одноразовую сессию на 1200 тиков. Повтор успешного запроса не списывает уровень. Протокол 3 требует обновить клиент и сервер; постоянный формат сохранений не менялся.
- `build` успешен; 60 JUnit-тестов, без ошибок и пропусков. Проверены 24 JSON-файла; тестовый код `smoke` не включён в JAR.
- Сервер с Terralith прошёл FakePlayer-проверку: прежние сценарии боя/талантов/сохранений/рецептов и новые сценарии цены пересборки, дистанции, повторного запроса, снятия лишнего здоровья, сохранения ресурсов, таймеров и инвентаря. Консольный маркер: `GUILDBOUND_COMBAT_SMOKE_PASS: combat, talents, save/reload, recipes, rebuild cost, distance, replay, health, preserved resources and inventory`.
- Сервер сохранён через `save-all flush` и штатно остановлен. Лог: `build/reports/verification/server-0.1.6-with-terralith.log`; консольный маркер System.out зафиксирован выше, он не записывается в этот лог.
- JAR: `build/libs/guildbound-0.1.6-dev.jar`, 121 852 байта. SHA-256: `D24165912038AF79A3C1C6FD12D5B7C03C9F06284A9F488CC4E8772562B38A9C`.
- Графическое меню и работа с настоящим подключённым клиентом ещё требуют ручной проверки по TESTING.md.

## Исправление 0.1.5.1 (изначально обозначалось 0.1.6-dev), 21 сентября 2026

- Убрана зависимость классового таланта от покупки общего. На уровне 3 классовый талант можно изучить первым, на уровне 2 доступны первые ранги обоих общих талантов. Требования к классу, уровню, бюджету, максимуму рангов и защита от повторных запросов сохранены.
- Обновлена подсказка условий в меню. Формат сохранений, сетевой протокол, уже купленные ранги и текущая выдача очков не менялись.
- Новая схема выдачи очков (5 при первом выборе класса, далее 1/2/3/5/10 по диапазонам уровней), разделение деревьев и рост цен записаны в ТЗ версии 1.2 как отложенная реализация.
- Сборка успешна, 56 тестов без ошибок. Добавлены сценарии первой покупки классового таланта на уровне 3 и общего таланта на уровне 2 для всех четырёх классов. Серверные и визуальные проверки для этого небольшого исправления повторно не выполнялись.
- По решению пользователя это исправление относится к 0.1.5.1; пользователь переименовал файл. Переименование файла не меняет внутреннюю версию старого JAR. Номер 0.1.6 далее используется для новой механики пересборки.
- Пользователь подтвердил остальные механики 0.1.5; сообщённая зависимость талантов исправлена этой версией.

## Завершённая версия: 0.1.5-dev, 21 сентября 2026

- Первые таланты в K: здоровье, восстановление стамины и классовый узел, по два ранга. Очки за уровни 2–5, серверные ограничения, защита от повторной покупки, сохранение при смерти/выходе. Старый формат прогресса читается без миграции; протокол сети 2 требует обновления обеих сторон.
- Все четыре материала кинжалов с заданными пользователем скоростью, уроном и прочностью. Рецепты золотого, железного и алмазного повторяют скриншоты, незеритовый улучшается через кузнечный стол с шаблоном.
- Два кинжала чередуют руки обычной кнопкой атаки; отдельные таймеры, полный урон клинка, сокращение перезарядки другой руки на 25% её полного времени при попадании. Ранние запросы не тратят стамину. Быстрые готовые удары обходят только ванильную задержку повторного получения урона.
- Подсказки показывают итоговую скорость оружия с бонусами персонажа; у лука — бонус урона. Воздушный рывок доступен вору и следопыту, не отменяет падение.
- Сборка успешна, 54 JUnit-теста без ошибок; проверены 24 JSON-файла и 82 ключа локализации. JAR не содержит проверочный source set `smoke`.
- Сервер с Terralith 2.5.8 загрузил 1300 рецептов и 1404 достижения. Проверка FakePlayer завершилась маркером `GUILDBOUND_COMBAT_SMOKE_PASS`: значения всех материалов, удары обеих рук при ещё активной ванильной неуязвимости, отказ повторному удару в том же тике, износ обеих рук, ровно два списания стамины, атрибуты талантов, сохранение/загрузка здоровья 21 из 22 и талантов, наличие четырёх рецептов. Сервер сохранён и штатно остановлен.
- Лог загрузки/остановки: `build/reports/verification/server-0.1.5-with-terralith.log`. Консольный маркер теста зафиксирован в отчёте `docs/VERIFICATION-0.1.5.md`.
- JAR: `build/libs/guildbound-0.1.5-dev.jar`, 115 640 байт. SHA-256: `7451A21C939B0849F6F5087900EDA809CA198E0C0F04CFBF262E4B567AAA0848`.
- Ручная проверка пользователем: вкладка и кинжал 0.1.3 присутствуют; особенности щита и описание 0.1.4 работают. Клиентские анимации двух рук, новые модели/подсказки/таланты и поведение при задержке сети в 0.1.5 ещё требуют проверки. FakePlayer не заменяет эти проверки.

## История предыдущих версий

## Текущая версия: 0.1.4-dev, 20 сентября 2026

- Подвижная защита воина: ходьба со щитом без замедления от использования предмета, бег за 16 стамины/с. Истощение прекращает бег, сохраняя блокирование. Клиентская обработка движения через NeoForge, расход ресурсов проверяет сервер.
- Управление описано во вкладке способностей с учётом переназначенных клавиш. Формат сохранений и сетевых пакетов не изменён.
- Сборка успешна; 34 JUnit-теста без ошибок, включая 5 новых сценариев расхода и восс щитотановления сом. JSON и 57 ключей локализаций проверены.
- Сервер с Terralith 2.5.8 загрузил существующий мир и новые ресурсы (1297 рецептов, 1401 достижение), выполнил `save-all flush` и `stop`, завершился с `BUILD SUCCESSFUL`. Лог: `build/reports/verification/server-0.1.4-with-terralith.log`.
- JAR: `build/libs/guildbound-0.1.4-dev.jar`, 80 569 байт. SHA-256: `D7BD828089884E81DB98AA921364060B7EABE7CE01F95AFBC2CA6EBD7D1A09DA`.
- Клиентское движение со щитом, отображение вкладки и модели кинжала ещё требуют ручной проверки. Серверный запуск выполнен без подключённых игроков. Следующий этап — начальное дерево талантов.

## История проверки 0.1.3

- Добавлена собственная творческая вкладка Guildbound со стойкой и железным кинжалом.
- Кинжал с ой атаки. Бонус вора +20%, с уровня 3 +30%; описание обновлено в меню.
- Сборка успешна. 2бъёмной моделью, рецептом и открытием рецепта; 4 урона, 2,4 атаки/с, без размашисто9 тестов без ошибок; проверены 13 JSON-файлов, 55 ключей локализации и подстановки.
- JAR: `build/libs/guildbound-0.1.3-dev.jar`, 77 329 байт. SHA-256: `D4A0C97865A3612F11AB1F7509D3FB2DDD747E28B94D7C06D7AA2B7317E70E80`.
- Серверный запуск 0.1.3 не выполнен: автоматическая проверка разрешений отклонила запуск Gradle из-за лимита Codex. Это ограничение проверки, а не зафиксированный сбой мода. Вкладка, модель, рецепт и бой требуют проверки в клиенте.
- Ручная проверка 0.1.2 пользователем: работают скрытность и скорость вора, штрафы брони и исключение воина, следопыт, усиления уровня 3. Это не подтверждает ещё не проверенные механики 0.1.3.

## История проверки 0.1.2

- Рабочие классовые бонусы: скорость, лук, незаметность при подкрадывании, защита воина и скорость его оружия. Усиление на уровне класса 3, включая самолечение мага.
- Тег тяжёлой брони и штраф скорости для зарегистрированных персонажей, кроме воина. Действуют только в выживании/приключении.
- Меню показывает текущие классовые бонусы и усиленное лечение. Формат сохранений не менялся.
- `build` успешен; 27 JUnit-тестов без ошибок (5 новых сценариев классового баланса). JSON и 53 ключа обеих локализаций проверены.
- Выделенный сервер NeoForge 21.1.250 с Terralith 2.5.8 загрузил существующий тестовый мир, выполнил `save-all flush` и `stop`; `BUILD SUCCESSFUL`. Лог: `build/reports/verification/server-0.1.2-with-terralith.log`.
- JAR: `build/libs/guildbound-0.1.2-dev.jar`, 72 953 байта. SHA-256: `0B7A382351BA10A30D8C1E55338CDF5903B6275FCF76CDDAE2CE871D1687A623`.
- Пользователь подтвердил улучшение внешнего вида 0.1.1. Новые механики 0.1.2 пока не проверены с подключённым игроком; запуск сервера и unit-тесты не заменяют пункты ручной проверки в TESTING.md.
- Следующие этапы: кинжалы, щит, таланты. Полные классовые деревья, магический конструктор, квесты, группы и генерация поселений ещё не реализованы.

## История проверки 0.1.1

Дата: 20 сентября 2026 года. Версия: `0.1.1-dev`.

## Исправления 0.1.1

- Меню K и регистрация: убран наклон плоскостей, слои фона и текста разделены для устранения полос. Регистрация оформлена четырьмя карточками с иконками.
- Кнопка анимации показывает текущее состояние; настройка сохраняется в клиентском конфиге.
- Компактный HUD в духе Aincrad: иконка класса, здоровье, мана, выносливость и полоса опыта. Напоминания способностей перенесены в правый нижний угол.
- Смягчены расходы стамины и задержка восстановления; добавлены сценарии непрерывного бега и бега с прыжками в тесты.
- Формат сохранений и сетевых пакетов не изменён.

## Реализовано

- Проект NeoForge 21.1.250 / Minecraft 1.21.1 / Java 21, Gradle Wrapper 9.2.1.
- Стойка гильдии, серверная регистрация четырёх классов с пробными подклассами.
- Сохранение и синхронизация прогресса, получение опыта и первые пять уровней.
- Формат прогресса для дальнейшего мультикласса, ограничение суммарного уровня 50.
- Прототип парящих панелей, открываемый по K, и HUD.
- Выносливость, мана, восстановление и истощение; серверные проверки расходов.
- Рывок R и самолечение мага G с серверными перезарядками.
- Английская и русская локализация, инструкция и план проверок.

## Подтверждено инструментальными проверками

- Java-код компилируется, JAR собран: `build/libs/guildbound-0.1.1-dev.jar` (66 350 байт).
- 22 JUnit-теста: 8 на прогресс, 4 на формат сохранений, 10 на ресурсы. Ошибок и провалов нет.
- JSON-ресурсы синтаксически корректны; наборы ключей и количества подстановок локализаций совпадают.
- Предыдущая версия 0.1.0: выделенный сервер загрузил Guildbound и создал обычный тестовый мир. Ниже серверные проверки относятся к 0.1.0; для 0.1.1 они повторно не выполнялись.
- Сервер с Terralith 2.5.8 создал мир `terralith-smoke`, seed 1729, и успешно загрузил его повторно.
- На локальном сервере с Terralith выполнены `save-all flush` и `stop`; получено `BUILD SUCCESSFUL`, процесс завершён.
- Серверная загрузка прошла без ошибки вызова клиентских классов.

Логи: `build/reports/verification/server-without-terralith.log` и `server-with-terralith.log`.
JUnit-отчёт: `build/reports/tests/test/index.html`.

SHA-256 JAR:

```text
3902AEDF6ECCD08928586A8034864E656A6BA607C1175FD196A3D7BD37A8B227
```

## Границы проверки

Пользователь проверил 0.1.0 в игре и предоставил скриншоты с полосами в меню и громоздким HUD. Новый интерфейс 0.1.1 ещё не проверен визуально в клиенте: сборка и unit-тесты не подтверждают качество отрисовки. Регрессионный список находится в TESTING.md. Движение, лечение и сохранение реального профиля после смерти требуют проверки в игре.

Попытка поставить стойку консольной командой в координатах `0 120 0` вернула `That position is not loaded`. Это не считается проверкой размещения блока; ручной пункт остаётся открытым.

Terralith 2.5.8 выдаёт предупреждения NeoForge о категории спавна аксолотлей в `alpha_islands` и `fractured_savanna`. Они не остановили тестовый сервер. Полная совместимость по всем биомам и будущим структурам пока не подтверждена.

Сервер проверялся без загрузки полного набора клиентских медиаресурсов (`-x downloadAssets` с локальной ссылкой на индекс ресурсов 17). Для запуска клиента нужно выполнить обычный `runClient`, который завершит загрузку ресурсов. Эта проверка не подтверждает готовность клиентского окружения.

## Исправления окружения

- Gradle использует `file.encoding=COMPAT`, чтобы Java 21 корректно читала argfile с кириллическим путём проекта в Windows. Исходный Java-код по-прежнему компилируется как UTF-8.
- Тестовый classpath включает точные библиотеки Minecraft для проверки codec.
- Консоль `runServer` подключена к стандартному вводу, поэтому сервер можно штатно остановить командой `stop`.

## Ещё не готово

Пассивные классовые бонусы, тяжёлая броня, талантные деревья, комбо, полноценное уклонение, конструктор магии, гримуары, квесты, группы, мирогенерация гильдий/городов, преступления, подземелья и боссы остаются следующими этапами общего ТЗ. Это технический прототип, а не завершённая первая играбельная версия.


Итоговая проверка 0.3.0-dev: 93 теста, 0 ошибок; 350 ключей RU/EN, наборы и подстановки совпадают. JAR 275568 байт, SHA-256 00C5B75B05CB94143CFED35D069665CEC93EE3750A7ED4E88E1A61D26C3DC72B. Тестовые Smoke-классы отсутствуют в JAR. Выделенный сервер с Terralith 2.5.8 проверен и остановлен; журнал: build/reports/verification/server-0.3.0-with-terralith.log.

## 0.3.1-dev
Объединены оба сообщения пользователя с тестированием 0.3.0: см. [RELEASE_0.3.1.md](RELEASE_0.3.1.md). Предыдущие изменения сохранены после прерывания, добавлены свитки, обработка ответа смены пути и отдельные круги. Сетевой протокол 10.

Итог 0.3.1: 98 JUnit-тестов, 0 ошибок; 476 ключей RU/EN, ключи и подстановки совпадают. Сервер с Terralith: GUILDBOUND_PROGRESSION_V2_PASS, GUILDBOUND_ALL_SKILLS_PASS, GUILDBOUND_EXPANSION_PASS, GUILDBOUND_COMBAT_SMOKE_PASS. JAR 295392 байт, SHA-256 80BAD0D30813C8CB68201811C9C7D1CB149551227FE9739B98C40A6AB309BE93. Визуальная проверка в игровом клиенте не выполнена.

## 0.3.2-dev
Интерфейс и рост параметров: RELEASE_0.3.2.md. 101 тестов, 0 ошибок. Сервер с Terralith прошёл регрессионные сценарии и остановлен. JAR 300474 байт, SHA-256 D30DE723C8236233825F0DF47A08CA22FAEC3CE9F856F2F39DDA58B5A30D694F. Визуальная проверка клиентом не выполнена.

## 0.3.3-dev
Настройки HUD, собственная еда и заготовка жажды, подсветка талантов и правки текста. Подробности: RELEASE_0.3.3.md. 102 теста и четыре серверные smoke-проверки с Terralith прошли; графический клиент не проверялся.

## 0.3.4-dev
Новый компактный HUD, отдельная клавиша настроек O, стамина над хотбаром, условные индикаторы потребностей, продление доступа к стойке и ответы поверх UI. Подробности: RELEASE_0.3.4.md. Серверные проверки с Terralith прошли; графический клиент не проверен.

## 0.3.5-dev
Подвесное знамя и тонкая геометрия HUD, отдельные ячейки кругов, новый значок голода, исправление последнего пакета отката. Принятые обычные квесты сохраняются; срочные со сроком сдачи, звёздочкой и двойной наградой. Все счётчики и таймеры доступны в HUD и журнале. 108 тестов, четыре серверных smoke-проверки с Terralith; протокол 13. Графический клиент не проверен. См. RELEASE_0.3.5.md.
